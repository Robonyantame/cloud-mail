# Cloud Mail 领域术语

本词汇表统一子域名邮箱功能讨论中的用语。

## Language

**基础域名**：
随机子域名邮箱所依附的固定域名，例如 `alice@x7k.example.com` 中的 `example.com`。

**随机子域标签**：
在基础域名前随机生成的一段名称，例如 `alice@x7k.example.com` 中的 `x7k`。

**随机子域名邮箱**：
使用随机子域标签与基础域名组成收信域名的邮箱，例如 `alice@x7k.example.com`，用于为不同网站提供独立收信地址。
_Avoid_：随机二级域名邮箱

**邮箱前缀**：
邮箱地址中 `@` 之前的部分，例如 `alice@x7k.example.com` 中的 `alice`。

**自定义子域名邮箱**：
使用管理员指定的子域标签与基础域名组成收信域名的邮箱，例如指定 `shop` 后得到 `alice@shop.example.com`。
_Avoid_：自定义二级域名邮箱

**可用子域**：
管理员完成收信配置并实际验证后，允许用于批量创建邮箱的子域；它的名称可以随机生成，也可以自定义。

**停用邮箱**：
已停止接收新邮件但仍归原用户所有的邮箱；旧邮件仍可查询，原用户可以重新启用该邮箱。
_Avoid_：删除邮箱
