# 子域标签复用于全部邮箱域名

2026-09-15 用户确认以既有 `domain` 列表作为基础域名集合，`subdomain_domains` 只保存标签，展开为所有「标签.基础域名」组合，取消单独的 `subdomain_base`，避免重复维护完整子域列表。代价是添加标签或基础域名会同时启用多个组合，管理员需逐个完成收信接入与验证；API、权限、永久地址占用和历史子域标记仍按完整域名处理，移除标签或基础域名不会释放旧地址或恢复宽松收信。
